// ? TypeScript install few command
/**
 * *-> npm i -D typescript
 * *-> npm add --save-dev @types/node
 * *-> npm i -D tsx
 * *->
 */

// ? Express.js
/**
    -> Fast (দ্রুত) → খুব দ্রুত কাজ করে
    -> Unopinionated (নিরপেক্ষ) → আপনার কোড কীভাবে লিখবেন সেটা জোর করে বলে না
    -> Minimalist (মিনিমাল) → অপ্রয়োজনীয় জিনিস নেই, একদম হালকা
    -> Web framework for Node.js → Node.js দিয়ে সহজে ওয়েব অ্যাপ বানাতে সাহায্য করে
 */

//? Overriding express api
/**
 * * If we add our own custome logic change by express default behaveour.This call express overriding
 * * when we use overriding express api given blow:
 * *-> Method
 * *-> Routing
 * *-> Response
 * *-> Middleware
 * todos: একই route আবার define করলে আগেরটাকে override করা হয়।
 *
 */

// ? Express Middleware type
/**
 * *-> Application type Middleware
 * *-> Route type Midlleware
 * *-> Error Handling Middleware
 * *-> Buit-in Middlewaree
 * *-> Third-party Middleware
 */

// ? Which type of task on persform the express middleware
/**
 *
 * *-> execute any code
 * *-> Make Change to the request and responce object
 * *-> can end the request and response cycle
 * *-> Call the next middleware function in the stack
 */
